---
const { id, title, price, image } = Astro.props;

const href = `/producto?id=${id}`;
---

<div class="product-card p-4 text-center">
    <a href={href}>
        <div class="w-full h-64 overflow-hidden mb-4 ">
            <img class="w-3/4 h-full object-cover rounded-xl mx-auto" src={image} alt={title} />
        </div>
        <h3 class="text-xl font-semibold mb-2 text-light-blue">{title}</h3>
        <p class="text-gray-700 mb-8">{price}</p>

        <a
        href=""
        class="border-solid border-2 border-strong-blue bg-white text-strong-blue hover:bg-strong-blue hover:text-white rounded py-2 px-4"
        >
            Ver más
        </a>
    </a>
</div>
