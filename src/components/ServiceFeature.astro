---
const { title, icon, subtitle } = Astro.props;
---

<div class="service-feature p-4 border rounded text-center">
    <i class:list={[["fa fa-2x w-6 h-6 text-primary"], [icon]]}></i>
    <h3 class="text-xl font-semibold text-primary">{title}</h3>
    <p class="text-text">{subtitle}</p>
</div>
